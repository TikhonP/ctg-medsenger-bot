amends "../app_config.pkl"

server = new Server {
  host = "localhost"
  port = 8080
  medsengerAgentKey = "1234"
  debug = true
}

db = new Database {
  user = "hitler"
  password = "lalalala"
  dbname = "ctg-medsenger-bot"
  host = "localhost"
}